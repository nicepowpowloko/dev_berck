function licorne(color){
    // on défini une variable dans la fonction qui permettra de recuperer le parametre defini pendant le chargement de la fonction


document.body.style.background = color;
// le paramètre color changera dynamiquement selon les informations definies lors de l'appel de la fonction

}
